# Simple Temp
```cpp
#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve(){
    
}

signed main()
{
    cin.tie(0); cout.tie(0); ios::sync_with_stdio(false);
    int t;cin>>t;while(t--)
        solve();

    return 0;
}
```

# Our Temp
```cpp
#include <bits/stdc++.h>
using namespace std;
#define tc    \
    int t;    \
    cin >> t; \
    while (t--)
#define all(v) v.begin(), v.end()
#define KERO ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define int  long long
#define ll  long long
#define sz(d) ((int)((d).size()))
#define yes cout<<"Yes"<<'\n';
#define no cout<<"No"<<'\n';
#define lp(i, n) for(int i = 0; i < n; i++)
#define ln <<'\n' ; 
const int INF = numeric_limits<int>::max();
typedef vector<ll> vi;
typedef vector<pair<ll,ll>> vp;
typedef vector<vector<ll>> vvi;
typedef vector<vector<pair<ll,ll>>> vvp;
void solve(){

}
int32_t main()
{
  KERO
  tc  
    solve();

    return 0;
}
```